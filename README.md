# ai-studio-server (archived)

This repository has been **merged** into the unified monorepo:

## https://github.com/lobrzut/ai-studio

Linux install (one command):

```bash
curl -fsSL https://raw.githubusercontent.com/lobrzut/ai-studio/main/linux/bootstrap.sh | sudo bash
```

Windows: clone `ai-studio` and run `Install.bat` / `Start.bat` from the repo root.

Do not use this archived repo for new installs.
